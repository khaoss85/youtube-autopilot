# Core dependencies
pydantic>=2.0.0
python-dotenv>=1.0.0

# HTTP requests
requests>=2.31.0

# YouTube API
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1

# Video processing
# Note: ffmpeg-python is a Python wrapper, but requires ffmpeg binary installed on system
# Install ffmpeg: brew install ffmpeg (macOS) or apt-get install ffmpeg (Linux)
ffmpeg-python>=0.2.0

# Scheduling (using APScheduler for simplicity - no external dependencies like Redis)
# APScheduler chosen over Celery because:
# - Simpler setup (no broker/backend required)
# - Good enough for single-machine automated workflows
# - Built-in persistent job stores (SQLite)
APScheduler>=3.10.0

# Optional: LLM clients (uncomment based on your LLM provider)
# openai>=1.0.0              # For OpenAI GPT models
# anthropic>=0.5.0           # For Anthropic Claude models

# Optional: TTS services
# elevenlabs>=0.2.0          # ElevenLabs TTS
# google-cloud-texttospeech  # Google Cloud TTS

# Optional: Trend data sources
# pytrends>=4.9.0            # Google Trends (unofficial)

# Development dependencies (optional)
# pytest>=7.4.0
# black>=23.0.0
# mypy>=1.5.0
# ruff>=0.1.0
